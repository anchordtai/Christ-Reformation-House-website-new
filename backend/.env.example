# Copy this file to .env and fill in your values. Never commit .env.
# Flutterwave live keys (Settings → API Keys in dashboard) – all stay server-side only.
# Public key (optional):  FLWPUBK-xxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx-X
# Secret key (required):  FLWSECK-xxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx-X  ← paste your live secret here
# Encryption key (opt):   FLWSECKxxxxxxxx  ← only if you add card encryption later
FLW_SECRET_KEY=FLWSECK-xxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx-X

# Optional – not used in current redirect flow; keep for future server-side use if needed
# FLW_PUBLIC_KEY=FLWPUBK-xxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx-X
# FLW_ENCRYPTION_KEY=FLWSECKxxxxxxxx

# Currency for donations (NGN, USD, etc.)
FLW_CURRENCY=NGN

# Frontend URL – where users are sent after Flutterwave checkout (no trailing slash)
FRONTEND_ORIGIN=https://christreformationhouse.org.ng

# Optional: other services
# JITSI_DOMAIN=meet.jit.si
# SENDGRID_API_KEY=
